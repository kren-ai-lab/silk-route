# Invalid example: schema_version is required and intentionally omitted here.
dataset:
  name: invalid_missing_schema_version
  modality: protein
  mode: query_first

query:
  value: "reviewed:true"

execution:
  enrich: false

export:
  output_dir: "examples/results/invalid_missing_schema_version"
  format: csv
